apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.app.name }}-config
data:
  {{- range $key, $value := .Values.app.env.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}